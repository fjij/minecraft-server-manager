version: "3"

services:
  db:
    image: postgres
    #restart: always
    environment:
      POSTGRES_PASSWORD: password
    volumes:
       - ./db:/docker-entrypoint-initdb.d
    ports:
      - "5432:5432" # host:container
